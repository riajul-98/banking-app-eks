apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: issuer
spec:
  acme:
    server: "https://acme-v02.api.letsencrypt.org/directory"
    email: "riajul5@hotmail.co.uk"
    privateKeySecretRef:
      name: issuer
    solvers:
    - dns01:
        route53:
          hostedZoneID: Z0921417PYJ6ZAUQ9WZ3
          region: eu-west-2
      selector:
        dnsZones:
        - "lab.riajul-devops.com"
    - http01:
        ingress:
          class:  nginx